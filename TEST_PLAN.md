# TEST_PLAN

## 测试目标

验证当前项目的核心功能、页面交互、接口行为与端到端流程，确保后端、微信小程序、PC 管理端和小程序内嵌管理端在常见使用路径下稳定可用。

## 测试范围

- 后端接口与数据库种子数据
- 微信小程序用户端、技师端和内嵌管理端
- PC 管理端页面与组件
- 端到端用户流程
- 基础兼容性、可访问性与响应式表现

## 环境准备

1. 安装依赖：

```bash
pnpm install
```

2. 初始化数据库与种子数据：

```bash
pnpm db:init
pnpm db:seed
```

3. 启动开发服务：

```bash
pnpm dev:api
pnpm dev:pc
```

4. 如需运行端到端测试，安装 Playwright 浏览器：

```bash
pnpm exec playwright install
```

## 基础验证

### 依赖与构建

- 执行依赖安装，确认无缺失依赖或锁文件冲突。
- 执行 `pnpm build:pc`，确认 PC 管理端可正常构建。
- 执行 `pnpm verify:all`，确认 PC 构建与 E2E 通过。
- 检查控制台是否存在 Vue、CSS、打包或接口相关错误。

### 静态资源

- 确认 `pc-admin/index.html` 页面标题、根节点和资源引用正确。
- 刷新 PC 管理端页面后，确认静态资源不会出现 404。
- 确认小程序页面、样式和图片资源在微信开发者工具中无缺失。

## 后端测试

### 数据库与种子数据

- 执行数据库初始化流程，确认 `backend/database/seed.sql` 可成功导入。
- 验证用户、门店、审核、评价等核心表存在必要测试数据。
- 验证重复执行种子脚本时不会产生不可接受的脏数据或唯一键冲突。

### API 行为

- 验证列表接口返回字段完整、分页或筛选参数有效。
- 验证详情接口在有效 ID 下返回正确数据。
- 验证无效 ID、缺失参数、非法参数时返回合理错误。
- 验证新增、编辑、删除或状态变更类接口的数据落库结果。
- 验证管理端写操作会写入 `admin_audit_logs`。

## PC 管理端测试

### 通用布局

- 打开 PC 管理端首页，确认 `AdminLayout` 正常渲染。
- 验证侧边栏、顶部区域、主内容区域在不同屏幕宽度下布局正常。
- 点击导航菜单，确认路由切换正确且高亮状态正确。
- 刷新页面后，确认当前页面可恢复渲染。

### DataTable 组件

- 验证表头、表格行、空状态和加载状态显示正确。
- 验证分页按钮可点击，页码和数据随操作变化。
- 验证上一页、下一页在首页和末页时禁用状态正确。
- 验证表格操作列按钮可见且点击后触发预期行为。
- 验证长文本、空字段、异常字段不会破坏布局。

### 核心页面

- `UsersView`：用户列表、搜索、筛选、权限编辑、启用管理入口。
- `StoresView`：门店列表、新增、编辑、状态切换。
- `ReviewsView`：评价列表、筛选、详情、回复、显示/隐藏。
- `AuditView`：操作日志列表、筛选、详情信息展示。
- `OrdersView`：订单列表、状态筛选、确认、核销、取消。
- `TechnicianPortalView`：技师摘要、近期预约、我的排班、新增排班、我的提成明细。

## 小程序管理端测试

### 入口与权限

- 从“我的”页面进入管理端，确认仅 `can_manage` 用户可见入口。
- 普通用户访问管理端时，应提示“暂无管理端权限”并返回上一页。
- 管理端所有请求都应带 token 和开发期 `x-demo-user-id`。

### 通用交互

- 打开 `pages/admin/admin`，确认经营看板、门店筛选、刷新按钮和横向模块 tab 正常显示。
- 逐个点击 12 个模块 tab：经营看板、多门店、服务项目、技师管理、技师排班、预约订单、提成结算、首页配置、内容营销、会员权限、评价管理、操作日志。
- 每个模块都应有加载态、空态或列表内容反馈，不应停留在“迁移中”。
- 切换门店后，支持门店过滤的模块数据应刷新。
- 点击刷新后，应重新加载 bootstrap 与当前模块数据。

### 通用编辑弹层

- 验证新增/编辑弹层可打开、关闭、输入、选择和保存。
- 验证 `input`、`textarea`、`picker/select`、`checkbox-group/checks`、数字字段都能正确回填和提交。
- 首页配置 JSON 字段输入非法 JSON 时，应提示格式错误且不提交。
- 保存成功后应显示提示并刷新当前列表。

### 模块覆盖

- 多门店：新增门店、编辑门店、默认门店、启停状态。
- 服务项目：新增项目、编辑项目、门店归属、价格、时长、上下架。
- 技师管理：新增技师、编辑技师、职称、评分、擅长方向、服务项目绑定、状态。
- 技师排班：新增单条排班、批量排班、门店和技师选择、容量、开放/关闭状态。
- 预约订单：确认、核销、取消订单，状态更新后列表刷新。
- 提成结算：新增/编辑提成规则，查看本期提成预估。
- 首页配置：新增/编辑配置、排序、启停、JSON payload。
- 内容营销：新增活动、新增文章，列表同时展示活动和文章。
- 会员权限：编辑后台角色和管理入口开关。
- 评价管理：编辑门店回复、显示/隐藏。
- 操作日志：列表展示操作人、对象类型、对象 ID 和详情摘要，不提供编辑入口。

## 技师端测试

- 技师入口仅对 `can_technician` 用户显示。
- 技师工作台可加载个人摘要、今日预约、服务业绩和预估提成。
- 我的排班可查看列表并新增排班。
- 我的提成可查看汇总和订单级提成明细。
- 无技师权限时应提示并返回。

## 端到端测试

### 管理端黄金路径

1. 打开 PC 管理端。
2. 进入用户管理页。
3. 搜索一个用户并查看详情。
4. 切换到门店管理页。
5. 新增或编辑一个门店。
6. 切换到评价管理页。
7. 对评价执行筛选并查看详情。
8. 切换到操作日志页。
9. 切换到技师工作台，确认摘要、预约、排班和提成区域可见。
10. 确认整个流程没有页面崩溃、控制台错误或异常跳转。

### 小程序管理端手工路径

1. 在微信开发者工具中启动小程序。
2. 从“我的”进入管理端。
3. 依次切换 12 个管理模块。
4. 对每个可编辑模块执行一次新增或编辑。
5. 对订单执行确认、核销、取消。
6. 对评价执行回复和隐藏。
7. 对会员执行角色或管理入口更新。
8. 检查审计日志出现对应操作记录。

### 页面级按钮与操作覆盖

- 每个页面至少验证一个主要操作按钮。
- 每个页面至少验证一个次要操作按钮。
- 每个主要按钮都要检查文字、图标、loading 状态与按钮容器的对齐关系。
- 每个弹窗验证打开、取消、关闭和确认提交。
- 每个表单验证成功提交和必填项失败提示。
- 每个列表页验证搜索、筛选、刷新和空状态。
- 每个危险操作验证确认或可逆路径。

### 异常路径

- 模拟接口失败，确认页面展示错误提示。
- 模拟接口返回空列表，确认空状态展示正确。
- 模拟慢请求，确认加载态展示正确。
- 输入超长文本、特殊字符和无匹配关键词，确认页面稳定。
- 模拟 401，确认小程序请求封装可清理 token 并重试登录。

## UI/UX 检查

### 视觉一致性

- 检查按钮、表格、卡片、表单、弹窗的间距和圆角是否统一。
- 检查主色、辅助色、危险色、成功色在不同页面是否一致。
- 检查字体大小、行高和标题层级是否清晰。
- 检查所有按钮、导航项、卡片入口中的文本、图标与容器布局是否水平和垂直居中。

### 响应式与小程序适配

- 在桌面宽度下检查 PC 管理端布局。
- 在平板宽度下检查表格、侧边栏和操作按钮。
- 在微信开发者工具常见机型下检查小程序用户端、技师端和管理端。
- 确认页面无明显横向滚动、遮挡或溢出；管理端横向 tab 仅在 tab 区域内滚动。

### 可访问性

- 验证按钮和表单控件具备可理解的文本。
- 验证键盘 Tab 顺序在 PC 管理端基本可用。
- 验证弹窗打开后焦点行为合理。
- 验证文字与背景对比度可读。

## 回归测试清单

- 用户管理列表、搜索、筛选、详情、状态操作。
- 门店管理列表、新增、编辑、状态操作。
- 评价管理列表、筛选、详情、回复、显隐操作。
- 操作日志列表与详情摘要。
- 通用表格分页、空状态、加载状态。
- PC 管理端导航与布局。
- 小程序管理端 12 个模块、门店筛选、通用编辑弹层、订单状态操作。
- 技师端摘要、排班和提成明细。

## 通过标准

- 核心页面可正常打开，无白屏。
- 主要按钮和表单流程可完成。
- 列表页搜索、筛选、刷新符合预期。
- 后端接口返回结构稳定，异常场景有合理提示。
- 构建、基础检查和端到端测试通过。
- 浏览器控制台和微信开发者工具控制台无影响功能的错误。
