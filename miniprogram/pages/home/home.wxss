.home-hero {
  min-height: 596rpx;
  padding: 38rpx;
  background:
    linear-gradient(150deg, rgba(255, 255, 255, 0.96), rgba(231, 247, 241, 0.72)),
    radial-gradient(circle at 76% 12%, rgba(185, 242, 223, 0.48), transparent 250rpx);
}

.home-hero .title {
  position: relative;
  z-index: 1;
  margin-top: 24rpx;
  max-width: 560rpx;
}

.home-hero .subtitle {
  position: relative;
  z-index: 1;
  margin-top: 18rpx;
  max-width: 560rpx;
}

.hero-button {
  position: relative;
  z-index: 1;
  width: 244rpx;
  margin: 32rpx 0 0;
}

.retreat-photo {
  position: relative;
  height: 252rpx;
  margin: 28rpx 0 8rpx;
  overflow: hidden;
  border: 1rpx solid rgba(200, 235, 222, 0.92);
  border-radius: 34rpx;
  background:
    linear-gradient(135deg, rgba(255, 255, 255, 0.92), rgba(231, 247, 241, 0.86)),
    radial-gradient(circle at 24% 22%, rgba(185, 242, 223, 0.72), transparent 130rpx);
}

.retreat-photo::after {
  content: "";
  position: absolute;
  right: 34rpx;
  bottom: -56rpx;
  width: 230rpx;
  height: 230rpx;
  border: 1rpx solid rgba(24, 166, 125, 0.08);
  border-radius: 999rpx;
  background: rgba(24, 166, 125, 0.08);
}

.moon {
  position: absolute;
  left: 42rpx;
  top: 42rpx;
  width: 112rpx;
  height: 112rpx;
  border-radius: 999rpx;
  background: radial-gradient(circle at 34% 32%, #ffffff, #b9f2df);
  box-shadow: 0 12rpx 34rpx rgba(24, 166, 125, 0.18);
}

.incense-line {
  position: absolute;
  left: 188rpx;
  top: 76rpx;
  width: 2rpx;
  height: 138rpx;
  background: rgba(16, 36, 30, 0.16);
  box-shadow: 32rpx 28rpx 0 rgba(16, 36, 30, 0.09), 64rpx -10rpx 0 rgba(16, 36, 30, 0.07);
}

.ritual-row {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 14rpx;
  margin-top: 22rpx;
}

.ritual-row view {
  min-width: 0;
  padding: 22rpx 0;
  border: 1rpx solid #c8ebde;
  border-radius: 999rpx;
  color: #18a67d;
  background: rgba(255, 255, 255, 0.86);
  font-size: 26rpx;
  font-weight: 800;
  text-align: center;
}

.activity-scroll {
  white-space: nowrap;
}

.activity-card {
  display: inline-block;
  width: 520rpx;
  margin-right: 22rpx;
  padding: 18rpx;
  border: 1rpx solid rgba(200, 235, 222, 0.9);
  border-radius: 34rpx;
  background: rgba(255, 255, 255, 0.92);
  box-shadow: 0 14rpx 36rpx rgba(16, 36, 30, 0.08);
  vertical-align: top;
  box-sizing: border-box;
}

.activity-cover {
  width: 100%;
  height: 260rpx;
  border-radius: 28rpx;
  background: #e7f7f1;
  filter: saturate(0.92) contrast(1.02);
}

.activity-tag {
  margin-top: 18rpx;
}

.activity-title {
  display: -webkit-box;
  margin-top: 14rpx;
  overflow: hidden;
  color: #10241e;
  font-size: 32rpx;
  font-weight: 800;
  line-height: 1.35;
  text-overflow: ellipsis;
  white-space: normal;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

.activity-bottom {
  display: flex;
  align-items: baseline;
  gap: 16rpx;
  margin-top: 18rpx;
}

.origin {
  color: #8aa099;
  font-size: 24rpx;
  text-decoration: line-through;
}

.article-card {
  display: flex;
  gap: 22rpx;
  margin-bottom: 22rpx;
  padding: 20rpx;
  align-items: center;
}

.article-cover {
  flex: none;
  width: 180rpx;
  height: 180rpx;
  border-radius: 30rpx;
  background: #e7f7f1;
  filter: saturate(0.92);
}

.article-body {
  min-width: 0;
  flex: 1;
}

.article-title {
  display: -webkit-box;
  margin-top: 10rpx;
  overflow: hidden;
  color: #10241e;
  font-size: 30rpx;
  font-weight: 800;
  line-height: 1.4;
  text-overflow: ellipsis;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

/* Phone auth overlay */
.phone-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 999;
  display: flex;
  align-items: flex-end;
  justify-content: center;
}

.phone-overlay-backdrop {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(16, 36, 30, 0.45);
  backdrop-filter: blur(6rpx);
}

.phone-overlay-card {
  position: relative;
  z-index: 1;
  width: 100%;
  max-height: 78vh;
  padding: 52rpx 48rpx 48rpx;
  border-radius: 40rpx 40rpx 0 0;
  background: #ffffff;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 24rpx;
  box-shadow: 0 -14rpx 48rpx rgba(16, 36, 30, 0.12);
  overflow-y: auto;
}

.phone-overlay-icon {
  font-size: 88rpx;
  line-height: 1;
}

.phone-overlay-title {
  color: #10241e;
  font-size: 36rpx;
  font-weight: 800;
  text-align: center;
}

.phone-overlay-desc {
  color: #6a7c75;
  font-size: 26rpx;
  text-align: center;
  line-height: 1.5;
  white-space: pre-line;
}

.phone-overlay-features {
  width: 100%;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20rpx;
  padding: 20rpx 0;
}

.phone-overlay-feature {
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: 18rpx 22rpx;
  border: 1rpx solid rgba(200, 235, 222, 0.9);
  border-radius: 24rpx;
  background: rgba(231, 247, 241, 0.5);
  color: #10241e;
  font-size: 24rpx;
  font-weight: 700;
}

.phone-overlay-feature-icon {
  color: #18a67d;
  font-size: 28rpx;
  font-weight: 800;
}

.phone-overlay-button {
  width: 100%;
  height: 90rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 0;
  border-radius: 20rpx;
  color: #ffffff;
  background: linear-gradient(135deg, #18a67d, #14d4a0);
  font-size: 30rpx;
  font-weight: 800;
  box-shadow: 0 14rpx 28rpx rgba(24, 166, 125, 0.28);
}

.phone-overlay-terms {
  color: #8aa099;
  font-size: 22rpx;
  text-align: center;
}
